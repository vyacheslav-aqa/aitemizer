{
    "name": "aitemizer",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "cy:open:aitemizer": "cypress open --e2e --config-file cypress.config.js --browser chrome",
        "cy:run:aitemizer": "cypress run --e2e --config-file cypress.config.js --browser chrome",
        "cy:run:smoke": "cypress run --spec 'cypress/e2e/tests/smoke.cy.js' --config-file cypress.config.js --browser chrome"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "cypress": "^13.16.1",
        "prettier": "^3.4.2"
    }
}